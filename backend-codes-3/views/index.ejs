<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="public/stylesheets/style.css">
</head>
<body>
    <div class=" main w-full min-h-screen bg-zinc-900">
    <div class="form p-10 text-white" >
        <form action="/create" method="post">
            <input class="block w-full rounded-xl px-5 py-3 bg-zinc-800" name="title" type ="text" placeholder="Title goes here">
            <textarea class="block w-full px-5 rounded-xl py-3 mt-2 bg-zinc-800 resize-none" name="details" placeholder="Detail goes here"></textarea>
            <input class="mt-2 bg-blue-800 rounded-xl text-sm p-5" type="submit" value="Create Task">
        </form>
    </div>
    <div class="task p-10 flex gap-4 flex-wrap ">
        <% if(files.length > 0){%>
            <% files.forEach(function(val){%>
                <div class="task w-72 rounded-xl bg-zinc-800 text-white p-5">
                    <h1><%= val%></h1>
                    <a href="#" class="text-blue-500 mt-3">Read more...</a>
                </div>
            <%})%>
        <%} else {%>
                <h2 class="text-white px-2">NO TASK YET ?</h2>
        <%}%>
  
    </div>
</div>
</body>
</html>